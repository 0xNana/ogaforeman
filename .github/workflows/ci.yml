name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          version: 0.11.32
          enable-cache: true
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - uses: actions/cache@v4
        with:
          path: ~/.cache/firebase/emulators
          key: firebase-emulators-${{ runner.os }}-${{ hashFiles('frontend/package-lock.json') }}
      - run: uv sync --all-extras --locked
        working-directory: .
      - run: npm ci --prefix frontend
      - run: .venv/bin/ruff check .
      - run: .venv/bin/ruff format --check .
      - run: .venv/bin/mypy app
      - name: Backend tests without backing services
        run: .venv/bin/python -m pytest -q -m "not backing_services"
      - name: Backend tests with durable backing services
        run: >-
          frontend/node_modules/.bin/firebase emulators:exec
          --only firestore,storage
          --project demo-oga-foreman-ci
          --config firebase.json
          ".venv/bin/python -m pytest -q -m backing_services"
      - run: .venv/bin/python scripts/run_evals.py --adapter fixture
      - run: .venv/bin/python scripts/run_capacity_baseline.py
      - run: bash -n infra/deploy.sh infra/rollback.sh infra/monitoring/apply.sh

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          version: 0.11.32
          enable-cache: true
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: uv sync --all-extras --locked
        working-directory: .
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: frontend/playwright-report/
          if-no-files-found: ignore
          retention-days: 7

  container:
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    steps:
      - uses: actions/checkout@v4
      - run: docker build --tag oga-foreman:${{ github.sha }} .
      - run: bash infra/smoke-container.sh oga-foreman:${{ github.sha }}
